'use client';

// This is a re-export of the AuthProvider from the hook file.
// This is to keep the pattern of having context providers in their own file.
export { AuthProvider } from '@/hooks/use-auth';
